{
  "name": "saysay",
  "version": "2.0.2",
  "description": "Speech mode for macOS: the agent speaks every response aloud through the `say` command, translating screen content into spoken language instead of reading it out verbatim. /saysay toggles it on, /saysay off exits. Ships a per-user phonetic dictionary plus serialized, ducked playback so parallel sessions speak one after another.",
  "author": {
    "name": "local"
  },
  "keywords": [
    "agent-skills",
    "saysay"
  ],
  "skills": "./skills/",
  "interface": {
    "displayName": "Saysay",
    "shortDescription": "Speech mode for macOS: the agent speaks every response aloud through the `say` command, translating screen content in...",
    "longDescription": "Speech mode for macOS: the agent speaks every response aloud through the `say` command, translating screen content into spoken language instead of reading it out verbatim. /saysay toggles it on, /saysay off exits. Ships a per-user phonetic dictionary plus serialized, ducked playback so parallel sessions speak one after another.",
    "developerName": "local",
    "category": "Productivity",
    "capabilities": [
      "Read"
    ],
    "defaultPrompt": [
      "Use the Saysay plugin."
    ]
  }
}
