# pattern-memory changelog

## [v2.0.0] - 2026-07-04

- Introduced Pattern Memory as a local-first implementation-pattern registry.
- Added the `pattern-memory` helper with `init`, `index`, `search`, and `validate`.
- Added a cross-agent skill that searches Pattern Memory before fresh implementation work while treating private precedents as recall-only.
