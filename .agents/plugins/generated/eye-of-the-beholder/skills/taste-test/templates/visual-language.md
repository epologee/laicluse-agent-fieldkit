# Visual language: [Product name]

The light, decision-log-first form of this file. Grow it one entry at a time as taste-test decisions get made, from gut reactions rather than upfront research. This is the same `visual-language.md` that art-director produces in its fuller, per-axis, cited form; if this project later runs art-director, that pass formalizes what has accumulated here. Until then, this file is the standard eye-of-the-beholder verifies against and the build-time discipline builds from, so the same direction is not re-decided every session.

## Direction so far

*A one-paragraph summary of the current visual stance, in plain words. Rewrite as it firms up. E.g. "Quiet and current: one editorial serif for display, humanist sans for UI, whitespace-and-hairline grouping instead of cards, a single ink accent used only for the primary action. Warm-neutral surfaces, no gradients."*

## Committed moves (by axis)

Fill an axis only once a decision has actually been made for it. Leave the rest empty; this file grows, it does not start complete. Axis names match art-director's fuller template so the two forms stay compatible.

- **Type**: [display / body / mono choices, once decided]
- **Color**: [dominant, accent, neutral; what each means; once decided]
- **Form**: [radius, borders, surface depth, shadow stance; once decided]
- **Motion**: [durations and eases, or "not yet decided"]

## Slop tells this product refuses

The direction is defined as much by what it avoids as what it does. List the generated-interface tells this product deliberately does not use, so a future session does not reintroduce them.

- [e.g. no purple-to-blue gradients]
- [e.g. no bordered-card grids; group with whitespace and hairlines]
- [e.g. no status dots that encode nothing]
- [e.g. no Inter-as-only-typeface default]

## Decision log

Every taste-test decision appends a row. Keep the reaction verbatim: the user's own gut-words are the calibration for the next decision, and they do not survive paraphrase.

| Date | Axis | Reaction (verbatim) | Move chosen | Slop tell avoided |
|------|------|---------------------|-------------|-------------------|
| [YYYY-MM-DD] | [Type/Color/Form/Motion] | ["too busy, feels cheap"] | [group with whitespace, one accent] | [bordered-card grid] |
